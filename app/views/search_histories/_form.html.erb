<%= form_with(model: search_history, local: true) do |form| %>
  <% if search_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_history.errors.count, "error") %> prohibited this search_history from being saved:</h2>

      <ul>
      <% search_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :search_item %>
    <%= form.text_field :search_item %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
